target/
plans/

# Docker keys (generated, sensitive)
docker/keys/*.key
docker/keys/*.hex
!docker/keys/.gitkeep

# Docker validator configs (contain sensitive endpoints)
docker/config/*/config.toml
!docker/config/*/config.example.toml

# dotenv
.env

# jetbrains
.idea
# direnv
.envrc

# mac os
.DS_Store

# We don't want to commit the dependency directory of charts
charts/*/charts

**/__pycache__/
